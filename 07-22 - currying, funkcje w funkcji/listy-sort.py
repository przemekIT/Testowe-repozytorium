# listy - sort ( reverse = True)
# sort(key=len) -> sortuje po długości
# sort(key=len, reverse = True)
# sorted(iterable, key = None, reverse = False) -> globalna, nie zmienia obiektu, trzeba przechwycić -> wypluwa LISTĘ

# sety - wiele zmiennych w jednej, niezmienialne, nieindeksowane, nieposortowane - można dodawać i usuwać
# 